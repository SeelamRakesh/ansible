- name: conditions demo
  hosts: local
  connection: local
  vars:
   - number_of_sessions: 7
  tasks: 
  - name: Ansible sessions
    ansible.builtin.debug:
     msg: "Ansible is 7 sessions"
    when: number_of_sessions >= 7