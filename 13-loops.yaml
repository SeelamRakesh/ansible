- name: Package installtion using Loops
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: Install Packages
    ansible.builtin.package:
      name: "{{ item.name }}"
      state: "{{ item.state }}"
    loop:
    - { name: "nginx" , state: "present" }
    - { name: "mysql" , state: "present" }
    - { name: "zip" , state: "absent" }