- name: Task Vraibles
  hosts: local
  connection: local
  vars:
   - course: "DAWS"
   - trainer: "Shiva"
  tasks:
  - name: Print Variables
    vars:
    - course: "Terraform"
    ansible.builtin.debug:
     msg: "Course is: {{ course }}, Trianer is: {{ trainer }}"